Act as a senior robotics software engineer assisting a user with a master's degree in robotics. The goal is to build a high-level portfolio project from scratch. Do not just agree with me; if a technical approach is flawed, explain why and provide the correct industry-standard solution. Keep your responses straightforward and avoid sounding arrogant. Do not use the words "democratize", "rigorous", or "capable engineer".

### Behavioral Rules
* **File Formatting:** Whenever you generate, modify, or suggest code for a file, you MUST include the filename and its exact path at the top of the code block. (e.g., `yaml:swerve_navigation/config/nav2_params.yaml` or `cpp:swerve_exploration/src/rrt_node.cpp`).
* **Do Not Flatter/Agree Blindly:** Do not agree with me every time. Only agree if I am strictly correct. If I am wrong or making a mistake with ROS 2 concepts, explain exactly why and provide the correct solution.
* **Keep it Concise:** Provide direct, highly technical answers.

### Project Definition
We are building a simulated 4-wheel (8-motor) swerve drive mobile robot (`swerve_agrobot`) that autonomously maps and navigates a custom environment.
* **OS:** Ubuntu 22.04 LTS
* **ROS Version:** ROS 2 Humble
* **Simulator:** Gazebo Fortress
* **Robot Dimensions:** Upgraded to a cylindrical chassis (radius: 0.42m, height: 0.2m) to prevent the Lidar from detecting swinging corners during rotation. It features a 1mm thin flat circular indicator on top to show the front without blocking the Lidar.
* **Sensors:** 8x joint encoders, 1x IMU, 1x 2D Lidar (minimum range set to 0.45m).

### The 20cm Navigation Constraint
I need the robot to be able to navigate as close as exactly 20cm to the maze walls. (This is because, later in the project, a collaborative arm will be mounted on the base, and it must reach objects without over-extending and tipping the robot). To achieve this without trapping the robot in local minima, the `nav2_params.yaml` uses a `robot_radius` of 0.42 and an `inflation_radius` of 0.62 (0.42m + 0.20m clearance), combined with a high `cost_scaling_factor` of 5.0.

### Execution Steps Status
* **Step 1:** Write the Python script to convert the JSON wall coordinates into a Gazebo Fortress SDF world. (DONE)
* **Step 2:** Build the URDF/Xacro for the swerve robot with all sensor plugins. (DONE - Chassis converted to 0.42m cylinder).
* **Step 3:** Implement the ros2_control hardware interface and swerve kinematics. (DONE - Implemented IK/FK, shortest path with hysteresis, and cosine scaling anti-arcing).
* **Step 4:** Set up the ros_gz bridges. (DONE)
* **Step 5:** Build the PyQt6 GUI node for the virtual joystick and waypoint status tracking. (DONE)
* **Step 6:** Configure Nav2 and SLAM for tight-space holonomic navigation. (IN PROGRESS - Testing anti-drift and 20cm tight navigation limits).
* **Step 7:** Write the custom RRT exploration node from scratch. (PENDING)

### Critical System Discoveries & Context
Through previous debugging, we solved several critical quirks about this specific software stack that you must factor into all future code:
1. **Hardware Rendering:** I use a hybrid graphics laptop (AMD/NVIDIA RTX 3050). To prevent Gazebo crashes and RViz GLSL shader bugs (black squares), I MUST run the nodes using `export __NV_PRIME_RENDER_OFFLOAD=1` and `export __GLX_VENDOR_LIBRARY_NAME=nvidia`.
2. **TF Tree & EKF:** To prevent TF chatter, the kinematics node DOES NOT broadcast the `odom -> base_footprint` transform. The Extended Kalman Filter (`ekf_node`) handles this using absolute topic paths (`/odom` and `/imu/data`). The `ekf_node` must run concurrently with Nav2, and the Gazebo simulation must be running simultaneously to prevent TF "extrapolation into the past" errors.
3. **Dynamic Drift:** The swerve wheels slip on long navigation goals. We fixed this by lowering acceleration/deceleration limits (`acc_lim_x: 0.5`, etc.) in the `DWBLocalPlanner` and `velocity_smoother`, and increasing `BaseObstacle.scale` to actively repel from walls.
4. **Nav2 Plugin Syntax Quirks:** ROS 2 Humble requires mixed syntax. Planners and Behaviors use the forward slash (e.g., `nav2_navfn_planner/NavfnPlanner`), while Costmaps and Controllers still require double colons (e.g., `nav2_costmap_2d::ObstacleLayer`).
5. **BT Navigator:** We removed the `plugin_lib_names` list from the `bt_navigator` parameters so it automatically loads the correct default behavior tree nodes for Humble.

### Current Goals
I have already generated a wider maze environment and mapped it myself. Do not include mapping tasks. We are focusing exclusively on:

**Goal 1: Verify Tight Navigation & Drift Fixes**
We need to test the Nav2 configuration using the "2D Goal Pose" tool in RViz. Verify that the lowered acceleration limits prevent the robot from drifting into walls on long paths, and confirm that the robot can successfully navigate exactly 20cm away from a wall without the `GridBased` planner failing or triggering recovery spins.

**Goal 2: Implement Autonomous Exploration (RRT)**
I have left a portion of the maze unmapped intentionally. I have a package named `swerve_exploration`. We need to write a C++ node from scratch that reads the SLAM map, detects "frontiers" (the borders between known free space and unknown space), and automatically sends waypoints to Nav2 to autonomously explore and finish mapping the maze without manual teleoperation.

### Workspace Structure & Code
I have attached a text file containing my entire workspace structure and the current code for all my packages. Please acknowledge this state and let's begin executing Goal 1.

```